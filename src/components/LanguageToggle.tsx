// src/components/LanguageToggle.tsx
"use client";

import React from "react";

export default function LanguageToggle() {
  // عربي فقط — لا يوجد تبديل للإنجليزية
  return <span className="px-3 py-2 text-sm text-gray-500">النسخة العربية </span>;
}
